from django.db import models

# Create your models here.

# Is not in use because we are getting tags from call metrics api
# class Tag(models.Model):
#     name = models.CharField(max_length=100)

#     def __str__(self):
#         return self.name
